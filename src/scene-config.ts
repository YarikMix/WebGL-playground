/* Константы подстройки сцены. Отдельный модуль, потому что ими пользуются и `src/sky/*`,
   и экраны: класть их в types.ts значило бы смешать значения с типами (при verbatimModuleSyntax
   это ловушка), а в src/auth/ — заставить слой сцены зависеть от слоя форм. */
import type { SunDirection } from './types';

/** Солнце главной: стоит за планетой, поэтому освещён узкий серп у кромки */
export const SUN_ORBIT: SunDirection = [0, 0.38, -0.925];

/* Солнце экрана входа: уходит вбок, поэтому граница дня и ночи становится почти вертикальной
   линией через центр экрана. Критерий подбора (§9 спеки) — терминатор проходит через центр
   карточки на 1440×900. Подобрано на экране под текущую геометрию карточки и лимба. */
export const SUN_DAWN: SunDirection = [0.92, 0.12, -0.68];

/* Одна длительность на оба конца: CSS-переход формы и доворот планеты в useFrame.
   Идеальной синхронности кадр-в-кадр не будет — часы разные, — но глаз ловит только
   грубое рассогласование, поэтому важно, чтобы число было одно. */
export const SWEEP_MS = 700;

/* Доворот планеты при переключении на регистрацию: свет должен полностью пересечь карточку
   и не больше — слишком мало, и терминатор не доедет до другого края, слишком много,
   и он проскочит и вернётся не туда. Подобрано на экране. */
export const SWEEP_ANGLE = 0.34;
